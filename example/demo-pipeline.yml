resource_types:
  - name: email
    type: docker-image
    source:
      repository: pcfseceng/email-resource
      tag: {{docker-tag}}

resources:
  - name: receive-an-email
    type: email
    source:
      imap:
        host: {{smtp-host}}
        port: {{smtp-port}}
        username: {{smtp-username}}
        password: {{smtp-password}}
        inbox: "INBOX"

  - name: send-an-email
    type: email
    source:
      smtp:
        host: {{smtp-host}}
        port: {{smtp-port}}
        username: {{smtp-username}}
        password: {{smtp-password}}
      from: {{email-from}}
      to: [ {{email-to}} ]

jobs:
  - name: receive-email
    plan:
      - get: receive-an-email
        trigger: true
      - task: read-email
        config:
          platform: linux
          image_resource:
            type: docker-image
            source:
              repository: concourse/buildroot
              tag: git
          inputs:
          - recieve-an-email
          run:
            path: sh
            args:
            - -exc
            - |
              cat recieve-an-email/subject
              cat recieve-an-email/body
              cat recieve-an-email/date
              cat recieve-an-email/version
              ls -l recieve-an-email/attachments

  - name: send-email
    plan:
      - task: prep-email
        config:
          platform: linux
          image_resource:
            type: docker-image
            source:
              repository: concourse/buildroot
              tag: git
          params:
            OUTPUT_SUBJECT_FILE_NAME: generated-subject
            OUTPUT_BODY_FILE_NAME: generated-body
          run:
            path: sh
            args:
            - -exc
            - |
              # ensure you esape the ${BUILD_ID} variable with leading \
              echo -e "Email resource demo on $(date): build \${BUILD_ID}" > email-out/${OUTPUT_SUBJECT_FILE_NAME}
              echo -e "Cheers!\n\n \
              Build ID: \${BUILD_ID} \n \
              Build Name: \${BUILD_NAME} \n \
              Build Job Name: \${BUILD_JOB_NAME} \n \
              Build Pipeline Name: \${BUILD_PIPELINE_NAME} \n \
              ATC External URL: \${ATC_EXTERNAL_URL}" > email-out/${OUTPUT_BODY_FILE_NAME}
          outputs:
          - name: email-out
      - put: send-an-email
        params:
          subject: email-out/generated-subject
          body: email-out/generated-body
